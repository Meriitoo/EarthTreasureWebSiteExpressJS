<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/styles.css" />
    <title>Home Page</title>
</head>

<body>

    <div id="wrapper">
        <header>
            <!-- Navigation -->
            <nav>
                <div>
                    <a href="/">Home</a>
                    <a href="/earth-treasure/dashboard">Dashboard</a>
                    <a href="/earth-treasure/search">Search</a>
                </div>

                {{#if isAuthenticated}}
                <!-- Logged-in users -->
                <div class="user">
                    <a href="/earth-treasure/create">Add Stones</a>
                    <a href="/auth/logout">Logout</a>
                </div>

                {{else}}
                <!-- Guest users -->
                <div class="guest">
                    <a href="/auth/login">Login</a>
                    <a href="/auth/register">Register</a>
                </div>
                {{/if}}
            </nav>
        </header>

        {{#if error}}
        <div>
            <div class="errorContainer">
                <p>{{ error }}</p>
            </div>
        </div>
        {{/if}}

        {{{body}}}

        <footer>
            <p>@EarthTreasureVaul</p>
        </footer>
    </div>
</body>

</html>